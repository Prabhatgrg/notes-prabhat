import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function ViewQueue() {
    return(
        <SafeAreaView>
            <Text>Queued Tasks Goes Here</Text>
        </SafeAreaView>
    )
}